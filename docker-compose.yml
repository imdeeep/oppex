services:
  server:
    build: ./server
    env_file: ./server/.env
    expose:
      - "8080"
    restart: unless-stopped

  node-proxy:
    build: ./node-proxy
    env_file: ./node-proxy/.env
    ports:
      - "3000:3000"
    depends_on:
      - server
    restart: unless-stopped
