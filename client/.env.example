# Local dev — point at the Node BFF
VITE_API_URL=http://localhost:3000

# Production (Amplify) — leave VITE_API_URL unset or empty in the Amplify console
# so API calls use same-origin paths (/auth/*) proxied to EC2.
# VITE_API_URL=
